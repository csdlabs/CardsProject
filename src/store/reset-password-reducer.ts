
type initStateType = {

}

const initState = {

}

export const resetPasswordReducer = (state : initStateType= initState, action: any) => {
    switch (action.type){
        default: return state
    }
}