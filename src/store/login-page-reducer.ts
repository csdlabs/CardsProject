

type initStateType = {

}

const initState = {

}

export const loginPageReducer = (state: initStateType= initState, action: any) => {
    switch (action.type){
        default: return state
    }
}

