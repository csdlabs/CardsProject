
type initStateType = {

}

const initState = {

}

export const registerPageReducer = (state: initStateType = initState, action: any) => {
    switch (action.type){
        default: return state
    }
}