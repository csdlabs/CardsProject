

type initStateType = {

}

const initState = {

}

export const newPasswordReducer = (state: initStateType = initState, action: any) => {
    switch (action.type) {
        default: return state
    }
}