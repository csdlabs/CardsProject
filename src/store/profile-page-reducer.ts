

type initStateType = {

}

const initState = {

}

export const profilePageReducer = (state: initStateType = initState, action: any) => {
    switch (action.type){
        default: return state
    }
}